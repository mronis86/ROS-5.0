import React from 'react';

/** Offline show uses WebSocket-only timing; drift UI is not used. */
const DriftStatusIndicator: React.FC<{ itemId: number; className?: string }> = () => null;

export default DriftStatusIndicator;
