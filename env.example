# Environment Configuration Example
# Copy this to .env.local for local development

# ===========================================
# VITE/REACT CONFIGURATION (Frontend)
# ===========================================
VITE_API_BASE_URL=http://localhost:3001
# For Railway deployment: VITE_API_BASE_URL=https://your-app.railway.app

# ===========================================
# NEON AUTH CONFIGURATION (Optional)
# ===========================================
# These are for Neon Auth integration (if you want to use it later)
# Get these from your Neon Console > Auth section
VITE_STACK_PROJECT_ID=your_neon_auth_project_id
VITE_STACK_PUBLISHABLE_CLIENT_KEY=your_neon_auth_publishable_key
# Note: STACK_SECRET_SERVER_KEY is for server-side only (not needed for frontend)

# ===========================================
# DATABASE CONFIGURATION (Backend)
# ===========================================
DATABASE_URL=postgresql://username:password@host:port/database
# Railway handles this automatically

# ===========================================
# DEVELOPMENT CONFIGURATION
# ===========================================
NODE_ENV=development

