# Legacy graphics live HTML (not deployed)

These static pages used to live under `public/` and were served as Netlify URLs like `/lower-thirds-live.html?eventId=…`.

**Current production path:** Graphics Links → **XML/CSV Feed Page (Railway)** (`/netlify-*-xml` React pages).

Kept here so nothing is lost. Safe to delete this whole folder once you are sure no old vMix bookmarks still need them.

Do **not** move these back into `public/` unless you intentionally want them on the live site again.

Also kept separately (still deployed, Railway/WebSocket): `public/vmix-*-live.html`.
