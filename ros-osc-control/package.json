{
  "name": "ros-osc-control",
  "version": "1.0.0",
  "description": "Run of Show OSC Control - Electron app that never sleeps, receives OSC commands to control events",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron . --inspect=5858",
    "build": "electron-builder",
    "build:win": "electron-builder --win --x64",
    "build:portable": "electron-builder --win portable dir --x64"
  },
  "keywords": [
    "electron",
    "osc",
    "run-of-show",
    "timer",
    "control"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.16.0",
    "dotenv": "^16.3.1",
    "osc": "^2.4.4",
    "socket.io-client": "^4.5.4"
  },
  "devDependencies": {
    "electron": "^39.8.5",
    "electron-builder": "^26.15.3"
  },
  "build": {
    "appId": "com.ros.osc-control",
    "productName": "ROS OSC Control",
    "directories": {
      "output": "dist"
    },
    "files": [
      "src/**/*",
      "package.json"
    ],
    "win": {
      "target": [
        {
          "target": "portable",
          "arch": ["x64"]
        },
        {
          "target": "nsis",
          "arch": ["x64"]
        }
      ]
    },
    "portable": {
      "artifactName": "ROS-OSC-Control-${version}-portable.exe"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    }
  }
}
